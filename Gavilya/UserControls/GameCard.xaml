<UserControl x:Class="Gavilya.UserControls.GameCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lang="clr-namespace:Gavilya.Properties"
             xmlns:local="clr-namespace:Gavilya.UserControls"
             mc:Ignorable="d" 
             Height="200" Width="170" Margin="0 10 10 0">
    <Border CornerRadius="10" Background="#FF46465C">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="25"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="25"/>
            </Grid.ColumnDefinitions>

            <CheckBox x:Name="CheckBox" Style="{DynamicResource CheckBoxStyle1}" Background="Transparent" Grid.Column="0" x:FieldModifier="public" Visibility="Hidden" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button Cursor="Hand" Style="{DynamicResource EditButton}" Content="&#xe9ef;" Foreground="White" FontFamily="FluentlyIcons" Height="20" Width="20" Background="Transparent" Grid.Column="2" Grid.Row="0"></Button>

            <Image x:Name="GameIcon" Grid.Row="1" Grid.Column="1" Height="70" Width="70" VerticalAlignment="Top" Margin="0,10,0,0"/>
            <TextBlock x:Name="Title" FontSize="17" Margin="0 0 0 60" Grid.Row="1" Grid.Column="1" FontWeight="Bold" Foreground="White" TextAlignment="Center" VerticalAlignment="Bottom"/>
            <TextBlock x:Name="Version" Margin="0 0 0 45" TextAlignment="Center" Foreground="White" FontSize="10" Grid.Column="1" Grid.Row="1" VerticalAlignment="Bottom"/>
            <Button Click="Button_Click" Cursor="Hand" Margin="0 0 0 10" Style="{DynamicResource PlayButton}" Grid.Row="1" Foreground="White" FontWeight="Bold" Grid.Column="1" Height="25" VerticalAlignment="Bottom">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="&#xeab5;" Margin="-30 0 10 0" FontFamily="FluentlyIcons" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                    <TextBlock Text="{x:Static lang:Resources.Play}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                </StackPanel>
            </Button>

        </Grid>   
    </Border>
</UserControl>
