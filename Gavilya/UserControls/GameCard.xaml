<UserControl x:Class="Gavilya.UserControls.GameCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lang="clr-namespace:Gavilya.Properties"
             xmlns:local="clr-namespace:Gavilya.UserControls"
             mc:Ignorable="d" 
             Height="255" Width="170" Margin="0 10 10 0">
    <Border MouseEnter="StackPanel_MouseEnter" MouseLeave="StackPanel_MouseLeave" Cursor="Hand" CornerRadius="10">
        <Border.Background>
            <ImageBrush x:Name="GameIcon">
                
            </ImageBrush>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
            </Grid.ColumnDefinitions>

            <CheckBox Cursor="Hand" x:Name="CheckBox" Style="{DynamicResource CheckBoxStyle1}" Background="Transparent" Grid.Column="0" x:FieldModifier="public" Visibility="Hidden" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button Cursor="Hand" Style="{DynamicResource EditButton}" Content="&#xe9ef;" Foreground="White" FontFamily="..\Fonts\#FluentlyIcons" Height="20" Width="20" Background="Transparent" VerticalAlignment="Center" Grid.Column="2" FontSize="15" Grid.Row="0"></Button>

            <!--<Image x:Name="GameIcon" Grid.Row="1" Grid.Column="1" Height="70" Width="70" VerticalAlignment="Top" Margin="0 -10 0 0"/>-->
            <StackPanel Name="Infos" VerticalAlignment="Bottom" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3">
                <TextBlock x:Name="Title" FontSize="17"  Grid.Row="1" Grid.Column="1" FontWeight="Bold" Foreground="White" TextAlignment="Center" VerticalAlignment="Bottom"/>
                <TextBlock x:Name="Version" Margin="0 0 0 10" TextAlignment="Center" Foreground="White" FontSize="10" Grid.Column="1" Grid.Row="1" VerticalAlignment="Bottom"/>
                <Button Click="Button_Click" Cursor="Hand" Margin="20 0 20 15" Style="{DynamicResource PlayButton}" Grid.Row="1" Foreground="White" FontWeight="Bold" FontSize="15" Grid.Column="1" Height="35" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="&#xeab5;" Margin="-20 0 10 0" FontFamily="..\Fonts\#FluentlyIcons" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <TextBlock Text="{x:Static lang:Resources.Play}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Button>
            </StackPanel>

        </Grid>
    </Border>
</UserControl>
